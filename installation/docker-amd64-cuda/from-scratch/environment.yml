name: pytoych-benchmark
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - hydra-core
      - tqdm
      - wandb
      - pre-commit
      - black
prefix: /opt/conda/envs/pytoych-benchmark
